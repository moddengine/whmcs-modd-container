{
    "name": "moddengine/whmcs-container-plugin",
    "description": "WHMCS modules for the Modd Hosting controller",
    "type": "project",
    "license": "MIT",
    "require": {
        "php": ">=8.1"
    },
    "require-dev": {
        "phpstan/phpstan": "^2.1"
    },
    "scripts": {
        "analyse": "phpstan analyse --no-progress --debug"
    },
    "config": {
        "sort-packages": true
    }
}
